Config.framework = 'esx' -- esx, oldesx, qbcore

Config.notify = 'esx' -- esx, qbcore, custom

-- That config it's only to work in that frameworks, you need to edit that code in your custom framework in the client.lua
